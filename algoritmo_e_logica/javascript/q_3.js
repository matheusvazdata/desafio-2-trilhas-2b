// Crie uma lista contendo 4 números e, em seguida, exiba todos os números na tela

let lista = [23, 30, 30, 55, 60];

for (let i = 0; i < lista.length; i++) {
    console.log(`${i + 1}º item: ${lista[i]}`);
}